name: "Build iScream app"

on:
  workflow_dispatch:
    branches: [main]

jobs:
  build_and_test:
    if: github.event_name == 'pull_request'
    name: Build and Test
    runs-on: macos-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5
    
  
